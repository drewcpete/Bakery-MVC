@using Bakery.Models;

<h1>All Vendors:</h1>
@if(Model.Count == 0 )
{
    <p>You currently have no vendors!</p>
}
@foreach(Vendor vendor in Vendors)
{
    <p><a href="/vendors/@vendors.Id">@vendor.VendorName</a></p>
}